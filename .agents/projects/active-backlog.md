# Aira AI — Projects & Active Backlog

## Known Technical Debt
- **Session-close tooling is incomplete on this Windows workspace (found 2026-08-04, `graphify` gap added 2026-08-06)**: `lefthook.yml` is present but no `lefthook` executable is on PATH, so hooks are no-ops. `gitleaks` is also unavailable, so the session-close credential scan falls back to four narrow patterns rather than a full scan. `graphify` (needed for `make wiki-refresh`) is also not on PATH and `graphify-out/` doesn't exist in this checkout at all — the wiki has never been built locally on this machine, so any session that changes code cannot refresh it here even though the second-brain-close flow calls for it. Install/expose all three tools before treating local pre-commit hooks, closeout secret scans, or "wiki reflects current code" as true on this machine.
- **Orphaned Database Tables**: Bot Flow Builder tables (`automations`, `automation_flow_runs`, `bot_flows`) still exist in the Supabase schema, although the backend and frontend engine code has been deleted.
- **Stale Wiki References**: ✅ Cleared 2026-06-24. The graph carried 183 ghost nodes for deleted bot-flow modules because `graphify update` refuses to shrink the graph without `--force`. Ran `graphify update . --force` (4285 nodes, ghosts pruned) + `make wiki` (182 articles). `make wiki-refresh` now uses `--force` permanently, so deletions self-prune going forward.
- **`pytest`/`pytest-asyncio` not in `backend/requirements.txt`** (found 2026-07-04 setting up a fresh worktree) — they're installed in the existing `backend/venv` but were added ad hoc at some point, not tracked. A brand-new checkout's `pip install -r requirements.txt` will NOT have them; had to `pip install pytest==9.1.1 pytest-asyncio==1.4.0` manually to match. Add both to requirements.txt.
- **Worktrees need manual per-worktree setup** — `backend/venv`, `frontend/node_modules`, and `backend/.env` are all gitignored and NOT shared across `git worktree` checkouts (each worktree is a separate directory). Also, the native `EnterWorktree` tool's default "fresh" mode branches from `origin/<default-branch>`, which will silently miss any local-`main` commits not yet pushed to origin — if local main is ahead of origin (check `git log --oneline -1 origin/main` vs `main` first), create the worktree manually via `git worktree add <path> -b <branch>` (branches from local HEAD) and register it with `EnterWorktree --path <path>` instead of `--name`.
- **`frontend/.env.local` is also gitignored and not shared into worktrees** (found 2026-08-06, alongside the `backend/.env` gap above) — needs at minimum `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (the **anon/public** key from Supabase Settings → API — never the `service_role` key from `backend/.env`; `NEXT_PUBLIC_*` vars get inlined into the client-side JS bundle, so a service_role key there is a real credential leak, not just sloppy config) and `NEXT_PUBLIC_API_URL=http://localhost:8000` to point at a local backend instead of the production Render URL it falls back to. Without it, `middleware.ts` throws `Your project's URL and Key are required` on every dashboard route (500, not a clean redirect) before any page component runs.
- **`numbers.py`'s `_QUALITY_MAP = {"HIGH": "green", "MEDIUM": "yellow", "LOW": "red"}` (used by the single-number `sync_number_from_meta`/`get_number_quality` path, i.e. the per-number "Sync Meta" button) may be mapping the wrong Meta value format (found 2026-08-06, not fixed — out of scope for the change that found it)**: `webhook.py`'s `phone_number_quality_update` handler and `failover.py`'s `QUALITY_MAP` both use `GREEN`/`YELLOW`/`RED` for the exact same conceptual field, and the new bulk `sync-from-meta` endpoint's WABA `phone_numbers` list edge also returns `GREEN`/`YELLOW`/`RED` (confirmed against Meta's response, see `_WABA_QUALITY_MAP` in the same file). If `HIGH`/`MEDIUM`/`LOW` is genuinely wrong for `get_number_quality`'s endpoint too, the per-number "Sync Meta" button has likely never correctly updated `quality_rating` from Meta's actual value — worth auditing before touching that path again, and worth a live check (call `get_number_quality` against a real number, log the raw `quality_rating` Meta returns) before assuming either mapping is right.

## Active Backlog

- **Pilot coexistence client's contacts/history never backfilled (found 2026-08-11)**: "Astrotamil Wa" was connected via the new WhatsApp Coexistence flow (Approach A) before Approach B's `request_coexistence_sync` trigger existed, so Meta was never actually asked to sync their phone contacts or pre-connection chat history. Live connection works fine (messages both ways flow correctly); this is only about the one-time backfill. Fix: a one-off call to `request_coexistence_sync(phone_number_id, access_token)` (`backend/app/services/meta_cloud.py`) using that tenant's already-saved `meta_phone_number_id`/`meta_access_token`. User was asked whether to do this and hadn't confirmed as of this session's close — check before running it, since it also matters whether the client still wants pre-connection history visible in Aira at all.
- ~~Paid Expert Handoff: staff has no way to see who paid~~ — **Done.** Direction question resolved as a separate dedicated page, not a `chat_handovers`/Conversations-pool reuse (that approach was tried and dropped — see commit `c2a6f2af` "notify staff pool on expert handoff payment, drop chat_handovers approach"). Shipped: `notify_pool` call on payment confirm, authenticated `GET` endpoint listing sessions by bucket (`0920972c`), `/dashboard/consultations` page (`b415f015`, `6b98fcce`).
- ~~Paid Expert Handoff: live-tested end to end, three bugs found~~ — **Done 2026-08-12.** `messages_reply_source_check` missing `expert_handoff` (migration 173, was causing a silent double-reply — see decisions/log.md), AI hard-muted post-payment leaving leads with no answer to follow-up questions (now stays live via a `_expert_handoff_paid_prompt_block`, mirrors the existing escalation pattern), no way to close out a paid session (added `resolve_expert_handoff_session` + "Mark Resolved"). Consultations page's `ChatThread` embed removed — it's a details-only surface now, collected fields render as a table. Full writeup in decisions/log.md 2026-08-12 and subsystem-notes.md.
- **No Razorpay refund path exists anywhere (found 2026-08-12)**: checked `payment_razorpay.py` and the Razorpay MCP tool set — zero create-refund capability. If an astrologer never follows up and a lead wants their money back, there's no button; it's a manual Razorpay-dashboard operation today. Bump up if this goes to real client volume.
- **No SLA / re-notify if staff ignores a paid-consultation alert (found 2026-08-12)**: `notify_pool` fires once when a lead pays. If everyone's offline or the notification gets missed, nothing re-notifies or escalates — the AI reassurance (see above) buys time but doesn't guarantee a human actually follows up. Worth a "still unresolved after N hours" re-ping before this scales past a pilot.
- **Intake `fields` "Choice" type is still unimplemented (found 2026-08-12, renamed 2026-08-13)**: the panel's field-type dropdown (`IntakeConfigPanel.tsx`, was `ExpertHandoffConfigPanel.tsx`) offers Text/Date/Choice, but Choice has no options-input UI — there's no way to define the actual option values — and even if there were, `extract_fields()`'s LLM extraction prompt (`intake.py`, was `expert_handoff.py`) only injects `key`/`label`/`type`, never `options`, so the model never sees the choice list either way. Picking Choice today does nothing but put the word "(choice)" in the extraction hint with zero choices behind it. **The original motivating example for this item — "which package: Basic/Premium/VIP" — was built 2026-08-13, but as a separate top-level `packages` structure (its own name/price/description, selected before fields are collected), not as a Choice-type field.** The `fields` Choice type itself is still unbuilt and still has no real use case in the current live config (all fields are Text/Date). Build when a tenant needs a genuinely fixed-option *field* (not a priced tier) — add the options-list editor to the panel and thread `options` through to the extraction prompt together.
- **Legacy `/api/v1/expert-handoff` route alias still mounted (added 2026-08-13, blocks nothing yet)**: `main.py` mounts both `/api/v1/intake` and `/api/v1/expert-handoff` on the same routers because Razorpay's dashboard has `/api/v1/expert-handoff/razorpay-webhook` registered as the live payment webhook URL for the tenant(s) using Paid Intake, and switching it isn't something a deploy can do atomically. Remove the two legacy `include_router` calls in `main.py` only after updating that URL in Razorpay's dashboard by hand and confirming a test payment still lands. No urgency — both prefixes work identically today.
- **Astro Tamil astrologer external dashboard delivery — still blocked (2026-08-07)**: Intake's `intake_sessions.status='paid'` (was `expert_handoff_sessions`, renamed 2026-08-13) is the intended hand-off point, but nothing pushes that data to Astrotamil's own astrologer dashboard, and nothing relays the astrologer's reply back to WhatsApp. Blocked on an external unknown — does Astrotamil's dashboard expose a webhook receiver, a REST API, or neither. Needs a conversation with the client before this can be designed, let alone built. The staff-visibility fix above (Conversations page) is a same-session stopgap that makes the feature usable today without this adapter, not a replacement for it.
- **Surface stale hot leads (waiting too long for a reply) somewhere real, after removing the Overview "Needs attention" panel (found 2026-08-01)**: the panel was deleted at the client's request — it just listed every result from `GET /analytics/hot-leads-stale` reading "no reply since last message" with no per-lead differentiation, not actionable. The underlying need (know which hot leads are about to go cold from neglect) is still real; a quick check of `/dashboard/conversations` found no "waiting for reply" sort/badge there either, so there's currently no surface for this at all. If rebuilt, put it where the reply action actually happens (Conversations) rather than back in Analytics. See decisions/log.md 2026-08-01.
- **Live-verify the new `tanglish_escalate_tamil` reply mode against a real lead conversation (found 2026-08-01)**: `_resolve_tamil_lock()` and the `leads.tamil_locked` persistence (migration 163, commit `24b30d58`) are covered only by unit tests with a mocked `db` — no real tenant conversation has confirmed the lock actually sticks turn-to-turn, or that a Tanglish message with heavy Tamil vocabulary but Latin script doesn't false-trigger it. See decisions/log.md 2026-08-01.
- **Three dead/parallel delete paths for callers/team members, confirmed unused (found 2026-07-31, re-confirmed 2026-08-08, still not removed — out of scope both times)**: `DELETE /api/v1/team/{user_id}` (`team.py`'s `remove_member`, `api.team.remove` in `api.ts` — no frontend caller), `DELETE /api/v1/callers/{caller_id}` (`callers.py:535`'s `delete_caller` — also no frontend caller, only deactivates), and the operator console's own `DELETE /operator/clients/{tenant_id}/team/{caller_id}`. Roles page's `deleteUser` → `DELETE /api/v1/rbac/users/{user_id}` (`rbac.py`) is the one active, real user-removal path — as of 2026-08-08 it hard-deletes the `callers` row and writes an audit log entry (see decisions/log.md). Verify with a repo-wide search before deleting any of the three dead ones; unifying them into the one real path is a legitimate future cleanup, just never scoped into either session that found them.
- **Make immediate and scheduled broadcasts preserve global lead scores consistently (found 2026-07-30)**: Scheduled broadcasts deliberately create a fresh `broadcast_lead_scores` row at the configured Cold floor without updating `leads.score` or `leads.segment`. In contrast, `routes/upload.py::bulk_send` currently upserts all recipients into `leads` with the Cold-floor values, so an existing Hot lead is reset to Cold when using Send Now/import. Split existing recipients from genuinely new rows (or otherwise preserve global score/segment) so both delivery modes create a new per-broadcast score while an existing lead's global score continues unchanged.

- **Astro Tamil's WhatsApp Business Account is Meta-restricted (found 2026-07-25, Meta-side, not fixable in this codebase)**: no outbound of any kind (including AI auto-replies) will send while this holds — every attempt fails with error `131031` "Business Account locked." User confirmed the restriction is real and current as of 2026-07-25. Check Meta Business Manager / WhatsApp Manager directly for lift status; nothing in the app can resolve this. The `webhook.py` misclassification bug this caused (wrongly flagging 56 leads' phone numbers as bad) is already fixed — see decisions/log.md 2026-07-25 — but the account restriction itself is still open and will keep blocking real sends until Meta lifts it.
- **Re-verify `broadcast_lead_scores` / `lead_tag_interest` post-reply update path (found 2026-07-26, stale docs corrected but underlying mechanism not re-traced)**: subsystem-notes.md's Scoring section previously claimed `compute_score(broadcast_context=...)` writes both the per-broadcast row and rolls up to `lead_tag_interest` — verified false this session (no such parameter exists on `compute_score()` today, no writer found in `ai_reply.py`/`scoring_engine.py`). The table is still seeded at send time (`broadcast_executor.py`/`upload.py`) at the tenant's configured Cold floor, so it's not fully dead, but whatever (if anything) keeps a per-broadcast score in sync with a lead's actual replies was not found. Needs a fresh trace before anyone relies on "per-broadcast scoring" being live — it may be silently frozen at its initial Cold score/segment after send.
- **Deploy and verify the 2026-07-13 dashboard RBAC enforcement slice**: deploy the local frontend/backend changes. Test a view-only role across re-engagement, tags, templates, subscription, team, and conversations; confirm disabled controls stay visible where intended and corresponding mutation requests receive 403. Confirm a role with only `outbound_leads.manage` can still access the Outbound Leads views and send broadcasts. Re-run `frontend npm run build` from a clean Node process state before deployment; the closeout build attempts emitted existing Sentry setup warnings but did not return a final exit result due to overlapping local Next workers.

- **Deploy and verify profile Settings visibility plus dashboard overview retry (created 2026-07-13)**: deploy the local frontend changes in `ProfileMenu.tsx` and `lib/api.ts`. Verify a role with `settings.view` sees Settings in the avatar menu but not the sidebar, and simulate/recheck a transient overview failure to confirm the dashboard retries before rendering its error card.
- **Explicit language-switch request not reliably honored, "tamil-la sollunga" (found 2026-07-13)**: live-tested today, 0/3 — the model did not switch to Tamil script despite an explicit request to, directly contradicting the 2026-07-06 finding of 3/3 on the same nominal scenario (model behavior isn't stable across sessions for this case). `_reply_script_mismatch()` (the working fix for silent switches, see decisions/log.md 2026-07-13) deliberately exempts explicit-request messages via `_is_explicit_language_switch_request()`, so this case currently gets **zero protection** from either fix. Proposed approach: extract the requested language from the regex match and apply the same mismatch-check-and-regen pattern using that as the target, instead of exempting outright. **Must tighten the request-detection regex first** — a naive version would false-positive on sentences that merely mention a language (e.g. "I live in Tamil Nadu" contains "in Tamil") and, unlike today's exemption-only behavior, would then actively force a wrong language switch rather than just skipping a safety net. Not yet built; needs false-positive testing before shipping.
- **`test_permission_gated_routers` failing on `main` (found 2026-07-13, pre-existing, unrelated to language-mirroring work)**: asserts `"Depends(require_owner)" in` `app_settings.py`'s source, but that source was already updated to `require_permission("settings.view"/"settings.manage")` per the 2026-07-13 RBAC decision (see decisions/log.md). Test appears stale relative to the code it's checking. Not fixed — out of scope for the language-mirroring session that found it.
- **Deploy and verify dashboard analytics RBAC fix (created 2026-07-12)**: local code now aligns `/dashboard` with `/api/v1/analytics/overview` by allowing `dashboard.view` or `analytics.view`, and stops retrying 403s as cold-start failures. Deploy the latest backend/frontend changes, then verify a read-only user with `dashboard.view` can load `/dashboard` without `GET /api/v1/analytics/overview 403` loops or the generic "backend may be waking up" screen.
- **Reconcile live Supabase migration ledger drift (created 2026-07-12)**: live project `Aira AI` has the functional schema effects for `135_subscription_request_date_range`, `138_tenant_rbac`, and `139_repair_tenant_rbac_schema_cache`, but `list_migrations` does not show those migration names. Before future clean-replay or branch-merge work, decide whether to create a ledger-only reconciliation, pull/diff from live, or leave documented drift. Do not blindly reapply data/DDL migrations just to make names appear.
- **RBAC read/write enforcement sweep after Roles UI refinement (created 2026-07-12)**: Roles now store distinct read vs write/manage keys and the sidebar shows tabs for read-only roles, but many dashboard pages and backend routes still need a deliberate audit to hide/disable edit controls and reject mutation APIs when the role has only the `*.view` permission. Priority examples: outbound leads, templates, numbers, knowledge, catalog, settings, telecalling upload/scheduled/notes, and any page where a manager should see the same tab as admin but not edit.
- ~~Apply and verify RBAC repair migration 139 on live Supabase~~ - **Functionally verified 2026-07-12**: live project `Aira AI` has `tenant_roles`, RBAC `tenant_users` columns, tenant_roles RLS policies/grants, Telecaller rows for both tenants, and a PostgREST schema reload was requested. Remaining issue is migration-ledger drift, tracked separately above.
- **Deploy and verify backend CORS-on-500 guard (created 2026-07-10)**: `backend/app/main.py` now has middleware that converts unhandled backend exceptions into JSON 500 responses inside the CORS middleware stack, with `test_health_endpoints.py` coverage proving Bloom Matrix receives CORS headers on unexpected errors. Code is committed as `b3e1fcd` (`cors fix`); verify it is live on Render before relying on it. Production template sync is currently unblocked by the Supabase `meta_waba_id` migration, but this guard should still be live to stop future real 500s from looking like CORS failures.
- ~~Catalog backend + real AI media recommendation flow (created 2026-07-07)~~ — **Done 2026-07-13**: backend disambiguation (migration 140, `catalog_retrieval.py`, `ai_reply.py` gate wiring, `routes/catalog.py` API surface) plus the frontend variant-group/attributes editor are built and pushed to `main`. See decisions/log.md "Catalog item disambiguation" for full detail and the two open items it left behind (below).
- **Partial embedding coverage can hide catalog items from the AI (found + accepted 2026-07-13, not fixed by design)**: `match_catalog_items` filters `embedding is not null`; the fallback to "list every ready item" only fires when retrieval returns **zero** candidates, not when it returns a partial subset. So a tenant with, say, 10 pre-existing (unembedded) items who then creates or edits just one item (auto-embedded on write) is in the worst state: retrieval returns only that 1 embedded item as "the match," and the AI silently loses visibility into the other 10 — worse than pre-feature behavior. User explicitly chose to accept this risk rather than fix it now (options discussed: mandatory per-tenant `POST /catalog/reindex` at rollout, or a smarter fallback trigger based on embedded-vs-total ready-item ratio). If this bites a real tenant, the fix is either of those two, not a mystery to re-derive.
- **Mixed grouped+ungrouped contention set drops the differentiating attribute (found 2026-07-13, untested edge case)**: `classify_gate` classifies a contention set of `{None, "vg-1"}` as `broad_browse` (lists names only), not `same_group`. If a stray ungrouped item happens to share a name with two real grouped variants (e.g. two "2BHK Apartment" rows under `vg-1` for Coimbatore/Chennai, plus one unrelated ungrouped item also named "2BHK Apartment"), the customer sees the name once with no location prompt — the city-naming directive is lost. Not broken (model still asks something), just a UX degradation with zero test coverage either way.
- **`reindex_catalog_items` docstring says "doesn't have one yet" but re-embeds every ready item unconditionally (found 2026-07-13, cosmetic)**: no `embedding is null` filter in the query. Harmless (idempotent, and arguably useful for the partial-coverage item above), but wastes Jina API calls/cost on every call and the docstring should say "re-embeds all ready items," not imply incremental backfill.
- **Migration numbering cleanup before any fresh DB replay**: there are currently two migration files with prefix `130` (`130_drop_legacy_booking_artifacts.sql` and `130_flat_price_unlimited_catalog.sql`). If the migration runner sorts lexically this may still work locally, but the numbering is ambiguous. Rename/re-number the flat-price migration to the next unused sequence before relying on a clean environment replay. **Related, found 2026-07-06**: a second collision at 131 (`131_pending_whatsapp_alerts.sql` teammate vs. `131_add_ai_voice_reply_setting.sql` local) was found and resolved by renumbering to 132 — see decisions/log.md. While investigating, confirmed via live `list_migrations` that `129_subscription_toggle_sync.sql` and BOTH `130_*.sql` files are still not applied to the live Supabase project (last applied tracked migration is `128_itemized_subscriptions`) — unclear if intentional or an oversight; `129_subscription_toggle_sync.sql` contains `DELETE`s against `feature_catalog`/`tenant_subscription_items`, so don't apply blind — confirm intent with the user first.
- ~~Define the future packaged limits for AI tokens, telecaller seats, and phone numbers~~ — **Partially resolved 2026-07-31**: telecaller seats and numbers pool are hard-enforced again (`rbac.py`/`numbers.py`, live purchased-quantity vs. live active-count check, no `hard_cap` row needed). AI tokens remain unlimited/informational — still open for that metric only. See decisions/log.md 2026-07-31 and subsystem-notes.md "Telecaller seats & numbers pool: hard enforcement restored".
- ~~"Failed to fetch" approving a subscription request~~ — **Fixed 2026-07-04** by the user directly (commit `96fbf37` "approve error fix"). Follow-on sweep completed the same day — see below.
- ~~Itemized-subscriptions billing cycle + pricing + dashboard-gating follow-up~~ — **Completed and pushed 2026-07-04** (commits `2efe76b`, `96d9b59`, `3705e79`, now on `origin/main`). See decisions/log.md "Itemized Subscriptions: Billing Cycle, Pricing Fixes, Dashboard Gating" for full detail. Backend 251 passing (same 38 pre-existing env-only failures before/after), frontend lint/tsc clean.
- **`maybe_single()` None-guard sweep — still not exhaustive**: 5 confirmed instances fixed across 2026-07-02 and 2026-07-04 (see subsystem-notes.md), all in the itemized-subscriptions code because that's where they kept getting hit by real traffic. `operator.py` has 20+ other `maybe_single()` call sites (mostly `tenants` single-column lookups guarded only by `if not X.data:`) that have **not** been individually verified safe — they're guaranteed non-None only if the referenced row is guaranteed to exist by the time the query runs, which hasn't been checked case-by-case. Recommended: either a full grep-and-verify sweep, or (better, since sweeps keep missing instances) introduce a shared `fetch_one()` helper every call site routes through so the None-check can't be forgotten.
- **QA audit — 20 fixes parked on branches** (`qa/critical` 3, `qa/high` 9, `qa/medium` 4, `qa/low` 2). The full 58-finding audit (2026-06-22) was reverted from main after a fix broke lead creation (the `opt_in_source` CHECK didn't allow the added `"whatsapp"` value); 12 safe fixes + the env-var leak fix were re-applied. Safety tag `pre-revert-backup` preserves the old HEAD. **Merge one branch at a time, test after each** ([[feedback_qa_revert]]).
- **Number spam / blocking — open product problem** (boss-raised 2026-04-21, unsolved). WhatsApp API numbers get Meta-blocked after ~10–20 reports; manual caller numbers get Truecaller/carrier spam-flagged. Naive "swap dead number" isn't seamless. Treat number-health monitoring + multi-number pooling + branded caller-ID (Truecaller for Business, Exotel/Knowlarity) as a first-class subsystem, not an afterthought — it gates the core lead-gen value prop.
- **Shared UI primitives** — no Button/Input/Badge/Tabs components yet; ~50+ buttons / ~20 inputs / ~8 tab groups are inline. Next planned design step. **Example of the cost (2026-07-08)**: the operator client-detail sidebar's fixed width (`220px`, then bumped to `240px`) is hardcoded independently in both `operator/(console)/client/[id]/sidebar.tsx` (the `<aside>`'s `w-[…]`) and `page.tsx` (the content area's `ml-[…]`) with no shared constant — any future resize must touch both files or the content silently overlaps/gaps against the sidebar.
- **Callback-notifications deferred polish** (non-blocking, from 2026-07-01 final review): lazy-load `api.callers.list()` only when `claimable_audience==='specific'` in `NotificationConfigPanel`; remove dead `cfg.events[key] ?? true` fallback; `push_allowed` reads `notification_config` on every `notify_user` (add a short TTL cache if it gets hot); add a tenant-wide "X of Y team members have push enabled" indicator (per-user `push/status` exists, needs a tenant rollup endpoint); push-tag collapse for simultaneous same-type pushes is a known v1 limitation.
- **Retire `lead_scorer.py`** (legacy two-pass scorer) — still called only on the AI-disabled branch; route that branch through `compute_score` instead. Low priority.
- **Render free → Starter ($7/mo)** — fixes cold-start tail and makes in-process APScheduler reliable (free tier sleeps after 15min → jobs skip). User action.
- ~~Monetization follow-ups (deferred, from 2026-07-01)~~ — **Superseded 2026-07-02, then superseded again 2026-07-04**: the admin-assigns-one-plan model these entries referred to no longer exists (see decisions/log.md "Itemized subscription pricing + client-driven approval flow"). Hard-cap enforcement is **no longer deferred** — implemented 2026-07-04 for telecaller seats, numbers pool, AI replies, and call minutes (outbound message sends deliberately stay track-only, by explicit user scope decision).
- ~~Admin-customizable subscription plans follow-ups (2026-07-02)~~ — **Moot 2026-07-04**: Feature Store (the plan-picker these follow-ups were about) was deleted outright and replaced by a read-only Entitlements view; there's no more "Change Plan" dropdown or plan-reassignment propagation concern. `.agents/context/subsystem-notes.md`'s Operator Console Monetization section needs another rewrite pass for the itemized-cart model — if anything still reads 2026-07-02 "single plan_id" language, it's stale, update on sight.
- **`feature/scoring-booking-refactor` branch** — unmerged; contains booking state-machine removal + `[COLLECT_DONE]` pattern (migration 072 already applied). Dynamic pricing is already on main; the state-machine removal is NOT. **Conflict note (2026-07-05)**: `main`'s `[COLLECT_DONE]` handling was deliberately removed this same day (see below) — merging this branch later would reintroduce it. Reconcile before merging.
- **Operator console dark mode (DEFERRED by user 2026-07-02)** — approach fully decided + specced in `docs/plans/operator-console-qa-and-features.md` Task 10: migrate tailwind hex tokens → channel-triplet CSS vars (light values byte-identical), `.dark` override scoped to the operator console root only, toggle in operator-sidebar. Ready to execute on request.
- **Operator console deferred polish (from 2026-07-02 final review, updated 2026-07-02 PM after Fleet/impersonation removal)**: (1) **`/alerts` still needs a server-side cache** — the bell polls every 60s from every operator page and re-runs `_build_fleet_rows`'s 2-queries-per-tenant loop (Fleet the PAGE is gone, but this shared helper still powers `/alerts` and still has the N+1 pattern); (2) ~~fleet CSV export formula-injection guard~~ — moot, Fleet page deleted; (3) palette "New Client" navigates to `/operator` but doesn't open the create wizard; (4) bulk action bar + summary pill share `fixed bottom-6 z-40` and can overlap; (5) `action-confirm.tsx` lacks Esc-close/focus-trap (matches confirm-dialog convention); (6) ~~impersonation `/end` audit~~ — moot, impersonation feature deleted entirely; (7) debounced audit search double-fetches when page>1; (8) alert severity "info" is typed but never emitted.
- ~~`main` unpushed AND behind origin~~ — **Resolved 2026-07-02**: reconciled and pushed (both directions). Surfaced a real bug during reconciliation — see "Production incident" in decisions/log.md — operator console top nav used a raw `<a href={item.href}>` that bypassed the new basePath. Fixed.
- **Hero section redesign — approved direction, not yet built (2026-07-02)**: brainstormed a "Live Console" hero for the landing page (`app/page.tsx`) to read as a technical product rather than a templated SaaS page — badge pill → mono live-status line (reusing `--success` token), static "dashboard mockup" screenshot → new `app/components/landing/HeroConsole.tsx` (dark terminal card reusing `--accent-gradient`, staggered mono event-log lines via the existing `.reveal`/`.reveal-delay-N` system, no new colors). User approved direction A before a production incident interrupted the session. Ready to implement on request — see the design writeup earlier in that session for exact copy/markup.
- ~~`maybe_single()` None-guard sweep (2026-07-02) — `client_overview`'s `tenant`/`owner_row` lookups~~ — **Fixed 2026-07-17**: this exact predicted landmine went live — a duplicate abandoned tenant with no `tenant_users` owner row caused a repeating 500 on the operator console every ~30s, confirmed directly in prod logs. Guarded both lookups (`if not tenant or not tenant.data` / `if owner_row and owner_row.data`) + added `tests/test_operator_client_overview.py`. The duplicate tenant itself was deleted. **Still open**: `operator.py` has 20+ *other* unguarded `maybe_single()` call sites, not yet individually verified — see the broader sweep item below, and consider the shared `fetch_one()` helper it suggests so this class of bug stops recurring one-at-a-time.
- **Replace the temporary shared Jina bridge key with per-client keys (created 2026-07-17)**: Jina embeddings went strictly per-tenant (see decisions/log.md), but the 2 existing tenants (`Astro Tamil`, `Bloom Matrix`) were both bootstrapped onto the same key so nothing broke on deploy. Go into the operator console and paste a distinct Jina key per tenant when convenient — otherwise per-client Jina cost still isn't actually separated, which was the whole point.
- **`app_settings.py`'s `_SECRET_KEYS` set is missing `gemini_api_key`/`openai_api_key`/`jina_api_key` (found 2026-07-17, not fixed)**: a different, separately-in-progress settings-update endpoint in `app_settings.py` (unrelated Facebook Embedded Signup work, mid-edit at the time) has its own `_SECRET_KEYS` allowlist for masking, and it's missing three of the five AI provider keys — if that endpoint is actually used to save those keys, they'd be stored/logged as non-secret. Not touched since the file had unrelated in-flight changes; worth a quick fix once that branch settles.
- **Voice Reply console UX follow-up (created 2026-07-17)**: now that Gemini TTS requires the tenant's own key (see decisions/log.md), the console's Voice Reply enable toggle should be disabled with an explanatory tooltip when no tenant Gemini key exists, instead of silently working today and silently failing once the key requirement landed. Similarly, the Groq key box's helper text should mention it also powers scoring/summaries/tuning/digests/coaching/compaction/briefs, not just reply models — those workloads have no separate model-choice UI, so the Groq box is their only visible on/off switch. Backend-only version of this change already shipped 2026-07-17; this is just the frontend polish.
- **Confirm apex domain `bloommatrix.in` (no www) reaches Aira (2026-07-02)** — the Vercel rewrite + basePath work was verified on `www.bloommatrix.in`. Meta's Tech Provider checklist lists the bare `bloommatrix.in/...` URLs (no www). Confirm the apex domain either redirects to `www` or has the same `/aira` rewrite applied directly — otherwise `bloommatrix.in/aira/privacy-policy` etc. 404 even though the www version works.
- **Confirm Vercel Deployment Protection off at the per-project level for Aira (2026-07-02)** — team-default "Vercel Authentication" was confirmed off (screenshot showed "Unprotected (0)"), which is sufficient for the Bloom Matrix → Aira rewrite proxy to work. Not separately confirmed inside the Aira project's own Settings → Deployment Protection page in case of a per-project override — low risk, quick to check.
- **SIM call tracking — dialing-path double-count (2026-07-02)** — the PWA cockpit's SIM-dial tap creates its own `sim_started` call_logs row; the new Android "Aira Sync" APK also creates its own row from the native call log. If a telecaller uses both (taps "Call" in the cockpit AND has the APK running), the same physical call can produce two rows. Not reconciled in Phase 1 — needs either (a) telling telecallers to dial from the native phone only and hiding the cockpit's SIM-dial button, or (b) matching-by-phone+timestamp reconciliation in `sim-cdr` ingestion so the APK's row updates the cockpit's existing row instead of inserting a new one. See [subsystem-notes.md](../context/subsystem-notes.md) → SIM call tracking.
- **SIM APK test-data cleanup (2026-07-02)** — lead `prem` (`b5a0a551-8c5c-4571-a68d-92035a9e5c9c`, `+919345679286`) was temporarily `assigned_to` the `Admin` caller (`13252cf0-06f8-4d3e-bbd5-277362a4cdde`) purely to end-to-end test the Aira Sync APK's lead-number filter. Unassign (or otherwise resolve) once testing is done — it isn't a real assignment.
- **SIM call tracking Phase 2 (recording upload) — built, reviewed, fixed, then reverted (2026-07-05)**, blocked on a real device constraint, not abandoned by choice alone. Full build exists in git history (`a4e9019`/`080c630`, reverted by `067d09b`) if picked back up. Blocker: whether a phone auto-records depends on which app is the default dialer — Google's Phone app only allows a manual per-number recording whitelist, while OEM native dialers (MIUI's stock Dialer, etc.) auto-record unrestricted. See decisions/log.md (2026-07-05 entry) and subsystem-notes.md → SIM call tracking for the full why. To revive this: needs a per-telecaller onboarding step verifying (a) their phone's default dialer, (b) that dialer's own auto-record setting is on, before assuming any recordings will exist to harvest — not something the app alone can guarantee across a telecaller fleet with mixed phone models.
- **Telegram save fix pushed but NOT confirmed live (2026-07-02)** — see decisions/log.md "Telegram settings save fixed" entry. Commit is on `origin/main`, but `aira-ai-5tfr` has `autoDeploy: no` (see the domain-migration incident entry), so a manual redeploy trigger (`mcp__render__update_environment_variables` no-op touch) + a live `list_deploys` check for status `"live"` on the right commit is still needed before telling the user it's fixed in production.
- **Stale notification deep-link URL (found 2026-07-03, not fixed — out of scope at the time)** — `frontend/hooks/useNotifications.ts:79` links to `/aira/dashboard/conversations?lead_id=...` (with the `/aira` basePath baked into the string), but the real route is `frontend/app/dashboard/conversations` and other call sites (e.g. `NotificationBell.tsx:246`, and the new WhatsApp admin alerts) correctly link to `/dashboard/conversations?lead_id=...` with basePath left for Next.js to apply automatically. Given the 2026-07-02 basePath migration gotcha above (raw `href`s don't get basePath auto-applied, but a *hardcoded* `/aira` prefix would double up if the surrounding `<a>`/router already applies it) — verify which failure mode this actually is before just deleting the `/aira` prefix.
- **Groq → Sarvam → Gemini migration — status (started 2026-07-05, revised 2026-07-18, revised 2026-07-29)**: AI WhatsApp reply generation stays operator-selectable per tenant (`sarvam-30b`/`105b`, Gemini, OpenAI, or Groq options in the operator console) — **superseded 2026-07-18: `sarvam-30b` confirmed deprecated by the operator, no longer the fallback default, see decisions/log.md.** `[COLLECT_DONE]` was separately and fully removed the same day (2026-07-05), unrelated. **Superseded 2026-07-18**: STT (transcription) and knowledge-base doc digitization (image/PDF OCR) moved from Sarvam to **Gemini `gemini-3.1-flash-lite`**; `analyze_call` (scoring + summary) moved from Groq to the same Gemini model — this reverses the 2026-07-05 decision to leave `analyze_call` on Groq. See decisions/log.md 2026-07-18 for the full rationale (Llama 3.3 70B's officially supported languages don't include Tamil) and live-verification detail. **Known limitation now moot**: the old "Sarvam Vision's `language` hardcoded to `en-IN`" note no longer applies — `sarvam_document_intelligence.py` (where that lived) is deleted. **Superseded 2026-07-29**: `scoring_engine.py`'s lead arc-scoring and `conversation_compactor.py` both moved from Groq to the same `gemini-3.1-flash-lite` — these two were the gap left by the 2026-07-18 pass (it only covered STT/OCR/`analyze_call`). Live-verified motivation: identical Tanglish text scoring 6/7/8 across leads on Groq. `call_digest.py`/`call_coach.py` are now the **only** remaining Groq `llama-3.3-70b-versatile` call sites, still deliberately out of scope — revisit if either shows the same Tamil-quality symptom.
- **`conversation_compactor.py` has zero test coverage (found 2026-07-29)**: no `test_conversation_compactor.py` exists. Not a regression from the Gemini swap — this gap predates it, just surfaced while touching the file. Add unit tests (mock `gemini_chat_completion`, cover initial vs rolling mode, empty-messages fallback) before next changing this file blind.
- **Two test files can't be collected by a bare `pytest` invocation (found 2026-07-18, not fixed)**: `test_ai_reply_llm_wiring.py` and `test_sarvam_chat_completion.py` are missing the `sys.path.insert(0, str(Path(__file__).resolve().parents[1]))` line most other test files in `backend/tests/` have, so `cd backend && pytest` fails to even collect them (`ModuleNotFoundError: No module named 'app'`) unless `PYTHONPATH=.` is set explicitly. Worked around for this session's verification; if CI doesn't already set `PYTHONPATH`, these two files are silently not running there either. Add the same `sys.path.insert` line both files' peers already use.
- **`sarvam_text_to_speech()` in `sarvam_client.py` is dead code (confirmed unused 2026-07-18)** — fully implemented, nothing calls it (Gemini TTS is the only active voice-reply path, see subsystem-notes.md). Flagged multiple times during the 2026-07-18 Gemini migration work but deliberately left alone since it wasn't in scope. Either wire it in (e.g. as a cheaper TTS alternative worth live-testing against Gemini's) or delete it.
- **Token/usage consumption monitoring for the operator console — designed but not built (2026-07-18)**: a detailed mockup exists (Artifact, not committed to the repo) for a "Token Consumption" card in the client Entitlements view — daily trend chart, per-feature breakdown, per-model table, all backed by real provider `usage` data every LLM client already receives and currently discards. No recording layer exists yet (would need a per-tenant/feature/model counter table + fire-and-forget writes in the wrapper functions). Also identified but not designed: STT minutes, TTS seconds, doc-digitization pages, and embedding tokens all bill in different units than chat tokens and would need their own meters on the same card. No go-ahead was given to build any of this — purely exploratory at this point.

---

## Recently Completed Features
- **Backend CI security test alignment (2026-07-13)**: Updated the static security regression around `app_settings.py` to assert RBAC settings permissions (`settings.view` / `settings.manage`) instead of the obsolete owner-only expectation. This resolves the previously recorded unrelated `test_security_hardening_static.py` backend-suite failure; full backend suite verification reported 396/396 passing with 3 warnings.
- **Catalog dashboard shell + AI media recommendation controls (2026-07-07)**: Added `/dashboard/catalog` as a new owner-facing dashboard module with Items, Media, AI Rules, and Insights tabs; linked it in the sidebar and mobile More menu; added operator client config controls for AI media recommendations and max images per reply; and labeled outbound AI-generated image messages in chat as "AI media recommendation." This is intentionally UI/settings only; real catalog persistence and AI delivery are in Active Backlog.
- **Newcomer Custom Subscription Periods & UI Polishing (2026-07-06)**: Added custom date range selection (defaulting to 15 days) for newcomer initial subscription requests. Proration is calculated on a daily duration basis (`duration_days / 30.0`), and approved request dates populate the active cycle start/end. Messaging features (broadcasts, conversations, segments, notifications) are grouped inside collapsible chevron dropdowns in the client cart. Hidden the Unit and Included input columns in the operator Pricing Catalog for flat modules, and shrunk/beautified all action buttons.
- **WhatsApp voice replies via Sarvam TTS + language-switch instruction fix (2026-07-06)**: `send_whatsapp_voice_reply()` sends AI replies as Sarvam TTS audio when a tenant's `ai_voice_reply_enabled` setting is on (migration 132, renumbered from a 131 collision — see decisions/log.md). Also reworded the LANGUAGE RULE's switch-request clause from a prohibition to a positive trigger, verified 3/3 vs. 1/3 on explicit mid-conversation language-switch requests. A related but distinct bug (silent/non-verbal script switches not being followed) was found during the same investigation and remains open — see Active Backlog above.
- **5-Minute Delayed WhatsApp Admin Notifications & Recheck (2026-07-05)**: Added a 5-minute configurable delay and verification check for WhatsApp admin alerts on lead segment transitions. Verified that the lead remained in the target segment after the delay before sending, and covered it with a robust mock-database unit test suite.
- **Move Knowledge Search Mode to Developer Console (2026-07-05)**: Removed the Smart/Exact/Hybrid search mode selector from the client-facing knowledge base dashboard and moved it exclusively into the Developer Console under Client Configuration. Added backend GET/PATCH endpoints to retrieve/update config settings with invalidate_cache and admin-only guards, and covered them with a python unit test suite.
- **Non-Gated Dashboard Access & Subscription Package View (2026-07-04/05)**: Resolved client console gating and sidebar filtering for newly onboarded/inactive tenants. Enabled direct landing on `/dashboard` with dynamic onboarding welcome cards, and client-side route guarding to redirect attempts to visit restricted pages. Package cards dynamically render itemized details.
- **Subscription → Client Toggle Sync, and Notification Fold-In (Migration 129, 2026-07-04)**: Approving a subscription request or calling `sync_client_toggles(db, tenant_id)` recomputes entitlements and synchronizes client console toggles (`enabled_features` in `tenants`) and `calling_provider` in `telecalling_config`. Old billing-derived keys are automatically removed to mirror the active subscription items, while non-billing-derived keys (e.g. `analytics`) survive untouched. Standalone `notifications` is bundled directly into `inbound_messaging` and `outbound_messaging`. Testing this requires passing `db` to `get_telecalling_config` and `save_telecalling_config` (which now accept an optional `db` parameter to prevent unit tests from triggering live Supabase connections via `get_supabase()`). Done and verified.
- **Itemized subscription pricing + client-driven approval flow (Migration 128, 2026-07-04)**: replaced admin-assigns-one-plan with a client-built cart (`tenant_subscription_items` + `subscription_requests`), admin Pricing Catalog + Packages tabs + Approval Queue, client onboarding gate + a standalone `/dashboard/subscription` page (linked from the sidebar, promoted out of Settings) + hard-cap enforcement on seats/numbers/AI-replies/call-minutes. Feature Store deleted, replaced by a read-only Entitlements & Usage view. Same-day follow-up work anchored the billing cycle to the purchase date (not calendar month), fixed included-quantity/flat-price bugs, and stopped top-up requests from re-gating an already-active tenant's whole dashboard — see decisions/log.md for both entries. **Pushed to `origin/main`** (`2efe76b`, `96d9b59`, `3705e79`); migration applied to live Supabase + verified. Backend 251/251 (excluding the 38 pre-existing env-only failures), frontend lint/tsc clean. Plan: `docs/superpowers/plans/2026-07-04-itemized-subscription-pricing.md`.
- **WhatsApp lead-segment admin notifications + template validation (2026-07-03, no migration)**: `whatsapp_notifications` sub-config in `notification_config`, `services/whatsapp_notify.py`, `growth.py` background-task hook, `NotificationConfigPanel.tsx` WhatsApp section; plus Meta template hard-rule validation (leading/trailing variables, sequential numbering) client+server side, `templates.py` REJECTED-status fix, and Indian+English-only language restriction. See decisions/log.md and subsystem-notes.md → "WhatsApp templates & admin notifications". Backend 256/256. Pushed to `main`.
- **Operator Console QA + 6 features (2026-07-02, no migrations)**: auth-check resilience (unreachable→retry screen, not ejection), destructive-action confirms + masked temp passwords, custom `OperatorToggle`, shared `lib/operator.ts` + vitest, fleet attention queue with real health scoring, alert-center bell (`GET /operator/alerts`), Ctrl+K command palette, scheduler Run-now (409 on paused), bulk Suspend/Activate, read-only tenant impersonation (mint-nothing, security review PASS). Merged to `main` (`a4fa029`, **unpushed**). Plan: `docs/plans/operator-console-qa-and-features.md`. Backend 241/241, vitest 11/11.
- **SaaS Monetization + Entitlements (Migrations 123–126, 2026-07-01)**: `feature_catalog`/`plans`/`tenant_subscriptions`/`tenant_usage_counters` + track-only metering + 4-step onboarding wizard + `EntitlementToggle` (on/off/locked/metered ring) + Feature Store + Billing & Usage view + Fleet cockpit. Merged to `main` (`1620969`, **unpushed**); migrations applied to live Supabase + verified. Plan: `docs/superpowers/plans/2026-07-01-monetization-and-console.md`.
- **Callback notifications + configurable push (Migration 122, 2026-07-01)**: claimable-only model (auto-reassignment removed), due/claimable/claim pushes, tenant `notification_config` (per-event push toggles, threshold, audience incl. specific callers, quiet hours), admin "Notifications" settings tab. Merged to `main`; migration applied to live Supabase + verified. Plan: `docs/superpowers/plans/2026-06-30-callback-notifications.md`.
- **Security & Multi-Tenancy**: RLS launch blocker (Migration 114) + performance checks (Migration 115).
- **Shift Time Overrides**: Multi-tenant shift hour schedules per caller (Migration 112) + ShiftTimeline ranges.
- **Call Scripts Cockpit integration**: Segment-based routing, branch outcomes, and scripts cockpit panel (Migration 111).
- **Contact Recycling Engine**: Automatic APScheduler recycling job to re-queue unreachable voice leads (Migration 111).

## Tooling Follow-ups
- **Workspace root MUST be `Aira.AI\Aira-Ai` (inner), not the outer `Aira.AI`**: the git repo, `.claude/commands`, `.agents/`, `Makefile`, and all code live in the inner `Aira-Ai`. Opening Claude Code / the IDE at the outer `Aira.AI` makes `/second-brain-close` etc. invisible (Claude scans `<cwd>/.claude/commands`, which doesn't exist at the outer level) and breaks the commands' relative paths. The outer folder has its own separate (near-empty) `.git`/`.agents` — ignore it. Always open at the inner folder.
- **`/aira-status` renamed to `/second-brain-close` (2026-07-15)** — same ritual, now backed by `scripts/second_brain_close.py` (shared with every other project using this pattern) instead of a hand-maintained duplicate per tool. Claude command, Codex skill, and the old `.agents/skills/` copy were consolidated into one. If a saved snippet/muscle-memory still says `/aira-status`, it no longer resolves — use `/second-brain-close`.
- **Local wiki refresh toolchain on Windows**: `graphify-out/` is now ignored/local-only, but this machine currently lacks `make` and `graphify` on PATH, so `/aira-wiki` cannot rebuild the wiki after a fresh clone/pull. Install/expose graphify + make, or add a Windows-friendly script target.
- **`npm run dev` (and any npm script with inline `VAR=value` syntax) fails under this Bash tool on Windows (found 2026-08-01)**: `frontend/package.json`'s `dev` script is `NODE_OPTIONS=--no-experimental-vm-modules next dev` — POSIX inline env-var-prefix syntax. npm always executes package.json scripts through the OS default shell regardless of what invoked `npm run ...`, and on Windows that's `cmd.exe`, which doesn't understand `VAR=value cmd` and fails with `'NODE_OPTIONS' is not recognized...`. This happens even when `npm run dev` itself was invoked from Git Bash (a real POSIX shell) — the outer shell doesn't matter, only npm's internal script-runner does. Workaround: bypass `npm run` for this script and invoke the underlying command directly from Git Bash, which *does* handle the prefix natively: `NODE_OPTIONS=--no-experimental-vm-modules npx next dev`. Also note: locally the app is proxied under `/aira` (see subsystem-notes.md basePath entry) — hitting `http://localhost:3000/dashboard/roles` 404s; use `http://localhost:3000/aira/dashboard/roles`.
- **Backend Python environments on this machine are corrupted beyond individual packages (found 2026-07-31)**: both the global Python 3.13 install (`AppData\Local\Programs\Python\Python313`) and `backend/venv` have site-packages with files silently missing (not version mismatches — the package directories are missing individual submodule files, e.g. `httpx/_client.py`, `groq/_base_client.py`, `pydantic_core/_pydantic_core`, `pip/_vendor/rich/console.py` were each absent on one or the other environment this session). Global `pip` still worked even when the packages it manages were broken; used `python -m pip --python ./venv/Scripts/python.exe install --force-reinstall --no-deps --no-cache-dir <pkg>` to patch the venv package-by-package as each import error surfaced. Pattern (likely cause, unconfirmed): looks like incomplete/corrupted installs rather than a version issue — possibly antivirus quarantine or an interrupted OneDrive sync on this Desktop-located repo path. If `pytest` fails to even collect with a `ModuleNotFoundError` on a submodule (not the top-level package) of a dependency that's clearly installed, suspect this before debugging the test itself — try the other environment (global vs venv) or a targeted `--force-reinstall --no-deps` first.
- **RESOLVED 2026-07-29**: `backend/supabase/migrations/151_conversation_leads_search.sql` appeared to be missing from the repo (existed in live `list_migrations` but no local file) — turned out it existed on `origin/main` in a separate, not-yet-merged session's commit, not actually missing. Arrived via merge later the same day. No longer an issue; see decisions/log.md for the merge/renumbering note (this fix's own migration got bumped to 152 to avoid the number collision).
- **`calls.py:245-268` inserts `payload.phone` with zero normalization** (the SIM-basic manual-call auto-create-lead path) — every other lead-creation path (`webhook.py`, `upload.py`, `telecalling_upload.py`) normalizes to `+91XXXXXXXXXX` before insert/lookup. Ruled out as the cause of the 2026-07-29 Shared Inbox duplicate-lead report (see decisions/log.md — that bug was a different, RPC-level issue), but this path is still a live latent risk: a telecaller manually dialing a number in a different format than an existing WhatsApp-created lead's stored phone would both miss the lookup and insert a second, differently-formatted row for the same real number. Fix: normalize `payload.phone` the same way `upload.py`'s `_normalize_phone` does before using it for lookup or insert.

## Session Follow-ups
- **SIM Basic rollout checklist (Completed)**: The SIM manual analytics/status model, graceful push UX, provider-switch confirmation, provider-aware Live Agent Status, and mobile `tel:` dialer fix have been fully restored, fixed, and pushed to `main` (commit `91abfe2`). Testing issues (unit tests/Windows encoding) are resolved. Migrations `120_calling_provider_and_push.sql` and `121_sim_manual_call_statuses.sql` have been applied to live Supabase (2026-06-29). Mobile backgrounding fixes (keepalive, sync context, lazy call log creation, bottom-sheet modal) pushed to `main`.
- **VAPID keys on Render (Action Required)**: VAPID keys have been generated and added to `backend/.env` locally. To enable push notifications in production, manually add `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, and `VAPID_SUBJECT` to the Render Dashboard environment variables. Also ensure `pywebpush` is in `requirements.txt` and installed on the Render backend.
- ~~Telecaller invite provider-aware fields~~ — **Moot 2026-07-31**: the Teams-page invite form this referred to (`/team/invite`) has been deleted outright (dead code, no frontend caller, also an unguarded seat-limit bypass — see decisions/log.md 2026-07-31). Telecaller creation is now exclusively the Roles page's Create User form (`rbac.py`), which is already provider-aware (phone required for SIM Basic, TeleCMI agent id shown only when `calling_provider === "telecmi"`).
- **Operator audit — label 2nd developer by email (deferred)**: the global Operator audit log (`/api/v1/operator/audit-logs`) filters `actor_role='system_admin'` (developer/operator activity only; tenant events stay in the per-tenant view). Aira has two owners; when a second `system_admin` id is added, label each developer in the Actor column by **email** (resolve `actor_user_id` via Supabase auth — no schema change; `system_admins` has only user_id + created_at). User chose email over a custom name column.
- **Google Ads attribution — live verification + "big version" (2026-07-05, feature shipped `04375d9`)**: The tag-based Google→WhatsApp attribution is live on `main` but NOT yet verified with a real WhatsApp message to a live tenant. (1) **Live test**: generate a link for `[GADS:test_run]`, send it to an active number, confirm the lead shows `origin=Ad / campaign=test_run` on Inbound Leads (backend logs `Google Ads referral: lead ... (tag=..., gclid=...)`). (2) **Big version — gclid→spend**: `gclid` is already captured+logged but inert; wire a Google Ads API pull to resolve gclid→real campaign/keyword/cost so `ad_campaigns.spend_inr` (and cost-per-lead in `build_ad_performance`) get real Google numbers, matching what Meta can show. (3) FB/IG DM Google-tag parsing not wired (WhatsApp only today) — extend if a client drives Google→IG/FB DM.
- ~~AI reply language mirroring — natural (non-verbal) script switch not reliably followed~~ — **RESOLVED 2026-07-13, live-verified 18/18**. Found 2026-07-06: when a customer silently switches script mid-conversation (Tanglish→Tamil or Tamil→English, no announcement), the model stayed in its prior style. Root cause: Sarvam-30B's own-prior-reply anchoring beats prompt instructions specifically for silent switches. **Attempt 1 (2026-07-06)**: strengthened LANGUAGE RULE wording — 0/3 live, failed. **Attempt 2 (2026-07-13)**: preemptive per-turn deterministic script tag (`_latest_message_script_note()`), tested in two prompt placements — 0/6 live across both, failed; kept in code anyway since harmless. **Attempt 3 (2026-07-13, WORKING FIX)**: post-reply check + isolated regeneration. `_reply_script_mismatch()` compares the customer's latest message script vs the generated reply's script (skips the check entirely for explicit switch requests, via `_is_explicit_language_switch_request()`, so the already-working 3/3 case isn't disturbed). On mismatch, `generate_reply()` fires ONE regen call via `_regen_target_instruction()` — critically, this call carries **no conversation history**, framed as an isolated "rewrite this reply" translation task rather than "continue the chat": feeding the wrong reply back into full history mostly failed (1/6, re-triggers the same anchor) whereas the isolated call fixed it 10/10. The target language is named explicitly ("English or Tanglish", not "Latin script") after a vague script-only instruction let the model drift into literal Latin (the dead language) once. **Final live verification**: 18/18 across all 6 scenarios (silent switch both directions, explicit request, no-switch × 3 languages), using the actual production functions end to end, real Sarvam-30B calls. See `ai_reply.py` (`_reply_script_mismatch`, `_regen_target_instruction`, `_is_explicit_language_switch_request`) and `test_ai_reply_lang_detection.py` (27/27 unit tests). Full backend suite 405/406 (1 pre-existing unrelated failure in `test_security_hardening_static.py`, from upstream commit `a54786b`, not touched by this fix).
- **WhatsApp admin-alert config misconfigured on tenant "Astro Tamil" (found 2026-07-06)**: `notification_config.whatsapp_notifications.recipient_phones` is set to a phone number that is ALSO a live test lead in the same tenant, and `template_id` points at `aira_connect` — a customer-facing "thanks for reaching out" template, not admin-alert copy. Result: admin alerts about other leads' segment changes land in that person's own WhatsApp thread looking like a broken auto-reply loop. Not a code bug — user said they'll reconfigure `recipient_phones` + swap/create a proper internal-facing template themselves; not yet confirmed done as of 2026-07-06.
- **`[COLLECT_DONE]` feature fully removed (2026-07-05)** — including the defensive strip (user verified live, no `ai_prompts` row references `[COLLECT_DONE]` anymore, so the leak risk that justified keeping it doesn't apply). See decisions/log.md for both entries. `leads.collected_data` will stay permanently empty going forward. **Still needs checking**: any tenant's re-engagement template using a custom variable beyond `name`/`phone` (`reengagement_service.py:193`) will now silently render blank instead of the previously-AI-collected value — nobody has checked the live `reengagement_steps`/template config for whether any tenant actually relies on this. **Cross-reference**: the unmerged `feature/scoring-booking-refactor` branch (below) also contains `[COLLECT_DONE]` logic — if that branch ever gets merged, it would reintroduce the pattern this removal just deleted. Flag this conflict before merging that branch.

## Meta Ads Manager follow-ups (2026-07-24, migrations 148+149, shipped to `main` `939b8f5`)
- **`ads_management` Advanced Access is the live-ad-creation blocker — SUBMITTED, PENDING Meta review.** The whole Create tab + status-toggle + budget-edit are built, unit-tested, and merged, but they can only run against **Aira's own test ad account** on Standard Access until Meta grants Advanced Access. **Nothing in the code changes when it lands** — only which ad accounts are authorized targets. Also needs `pages_manage_ads` + a Facebook Page (fetched via `GET /act_<id>/promote_pages`, stored on `ad_campaigns.page_id`). When approval arrives, run the live smoke-test checklist below.
- **Live smoke test NOT yet done (needs the above or a test account)**: the actual Meta round-trip (create campaign→adset→creative→ad; pause/activate; budget edit) is unverified in CI — no network to Meta in the dev sandbox, and approval is pending. Checklist is in `docs/superpowers/plans/2026-07-24-meta-ads-create-management.md` Task 7 Step 3: confirm the Page dropdown populates from `/pages`, image upload returns a hash from `/media`, publish writes an `ad_campaigns` row with `created_via='aira'` and the campaign appears in the test account's Ads Manager, then toggle/budget-edit reflect in Meta.
- **Media upload proxies through the backend** (`POST /meta-ads/media` → `upload_image` → Meta `/adimages`) — the frontend never gets the Ads token. If large-image uploads time out on Render's free tier, that path (60s httpx client) is the place to look.
- **Objectives beyond Click-to-WhatsApp are descoped** (Website Traffic/App Promotion need a destination-URL/app-link field + no WhatsApp attribution; Leads/Sales/Awareness need Lead Forms / Conversions API / Pixel). Each is a clean follow-on that extends the Create wizard's Objective step without rebuilding the rest.

## Ad-Creative Attribution follow-ups (2026-07-23)
- **Replace lead-level conversion counting with message-level ad attribution events (identified 2026-07-31)**: today `leads.attributed_ad_creative_id` is first-touch-only, so repeat clicks/messages by the same phone are not counted as separate Aira interactions and a later Ad B is missed. Add account-scoped `ad_attribution_events` keyed idempotently by inbound Meta message id, plus per-ad `ad_tracking_codes` as a fallback when `referral.source_id` is absent. Keep one lead record, report both unique leads and attributed ad conversations, use event timestamps for date filters, expose new-vs-returning/code-recovered/unattributed counts, and rename absolute “No Message” to an explicitly unconfirmed/unmatched metric. If both referral and code are absent, retain an unattributed event/manual-assignment path; if the whole webhook fails, only aggregate reconciliation against Meta-reported conversations is possible. Harden webhook ingestion with durable raw-event persistence, retries, and a dead-letter/alert path before claiming exact message counts.
- **`ad_insights_daily` had 0 rows for the live "Astro Tamil" tenant despite everything else verified correct**: migration applied, `meta_ads_access_token`/`meta_ads_account_id` confirmed saved (right lengths, right timestamp), token independently verified live against Meta's Graph API (`ads_read` granted, real `inline_link_clicks`/`spend` returned for the account). Render log search across a live window did not conclusively show whether `ad-insights-sync` had fired with valid credentials yet or hit a silent error. The "Sync Now" button (`POST /inbound-leads/ad-sync-now`) was added specifically to close this loop with an immediate, non-swallowed error message — **use it first** before re-investigating this class of issue, and update this entry with the actual root cause once confirmed.
- **Meta Conversions API** (sending qualified-lead/hot-lead/converted/purchase events back to Meta for ad optimization) — explicitly deferred, not built. Would bolt onto the existing lead-status transitions without changing the attribution design.
- **Lead conversion/revenue tracking does not exist** — needed before the Ad Performance tab's Sales/Revenue/ROAS columns can show real numbers instead of `—`. No `leads.converted_at` or deal-value field currently exists.
- **`lefthook` and `gitleaks` binaries not on PATH in this dev environment** (found by `scripts/second_brain_close.py`, 2026-07-23) — `lefthook.yml` is present but hooks are silently no-op'ing (nothing enforces pre-commit checks locally right now), and credential scanning falls back to 4 narrow patterns instead of full gitleaks coverage. Install both (`lefthook install` after getting the binary on PATH; gitleaks via https://github.com/gitleaks/gitleaks — no Homebrew on this Windows machine, use a released binary or `go install`).

## Operator console QA checklist — only partially run (2026-07-29)
- User handed over a 17-point live QA checklist covering the confirm-dialog unification, Team/Management merge, wipe-leads merge, and several dead-code/small fixes (see the commits around `bff098a`/`fd9a2a0`/`b738d5b`/`126cf79`/`cdd6bee`/`952bdf8` in git log for what's being verified). Only the sign-out flow was reached (in progress) before the session got pulled into two side-quests: building custom-date-range support for Analytics "Daily Activity" + scrollable token charts (see decisions/log.md 2026-07-29 entry), then a dashboard alignment bug fix. **Still unverified**: pause-scheduler-job confirm modal, suspend/activate confirm+bulk, reset-password confirm, Data Ops clear-leads (typed-name confirmation + real record counts + the ported `app_settings.broadcast_history` cleanup step), Team tab (owner info/stats/roster/delete-caller), Management tab (down to 2 cards only), TTS/STT labels on Entitlements usage meters, WhatsApp token badge de-duplication on Health tab, Config tab usage-tile removal, Scheduler-vs-Clients system-health-card placement, sticky headers on client-detail tabs, Audit Log/Subscription Requests client-name links.
- Login credentials + a working local stack (backend `venv`, frontend `.env.local` pointed at the live Supabase project) are already set up from this session — see the two new bullets in subsystem-notes.md's "Local dev workspace" section before re-running this.
