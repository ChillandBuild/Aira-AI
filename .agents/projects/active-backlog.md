# Aira AI — Projects & Active Backlog

## Known Technical Debt
- **Orphaned Database Tables**: Bot Flow Builder tables (`automations`, `automation_flow_runs`, `bot_flows`) still exist in the Supabase schema, although the backend and frontend engine code has been deleted.
- **Stale Wiki References**: ✅ Cleared 2026-06-24. The graph carried 183 ghost nodes for deleted bot-flow modules because `graphify update` refuses to shrink the graph without `--force`. Ran `graphify update . --force` (4285 nodes, ghosts pruned) + `make wiki` (182 articles). `make wiki-refresh` now uses `--force` permanently, so deletions self-prune going forward.

## Active Backlog
- **QA audit — 20 fixes parked on branches** (`qa/critical` 3, `qa/high` 9, `qa/medium` 4, `qa/low` 2). The full 58-finding audit (2026-06-22) was reverted from main after a fix broke lead creation (the `opt_in_source` CHECK didn't allow the added `"whatsapp"` value); 12 safe fixes + the env-var leak fix were re-applied. Safety tag `pre-revert-backup` preserves the old HEAD. **Merge one branch at a time, test after each** ([[feedback_qa_revert]]).
- **Number spam / blocking — open product problem** (boss-raised 2026-04-21, unsolved). WhatsApp API numbers get Meta-blocked after ~10–20 reports; manual caller numbers get Truecaller/carrier spam-flagged. Naive "swap dead number" isn't seamless. Treat number-health monitoring + multi-number pooling + branded caller-ID (Truecaller for Business, Exotel/Knowlarity) as a first-class subsystem, not an afterthought — it gates the core lead-gen value prop.
- **Shared UI primitives** — no Button/Input/Badge/Tabs components yet; ~50+ buttons / ~20 inputs / ~8 tab groups are inline. Next planned design step.
- **Callback-notifications deferred polish** (non-blocking, from 2026-07-01 final review): lazy-load `api.callers.list()` only when `claimable_audience==='specific'` in `NotificationConfigPanel`; remove dead `cfg.events[key] ?? true` fallback; `push_allowed` reads `notification_config` on every `notify_user` (add a short TTL cache if it gets hot); add a tenant-wide "X of Y team members have push enabled" indicator (per-user `push/status` exists, needs a tenant rollup endpoint); push-tag collapse for simultaneous same-type pushes is a known v1 limitation.
- **Retire `lead_scorer.py`** (legacy two-pass scorer) — still called only on the AI-disabled branch; route that branch through `compute_score` instead. Low priority.
- **Render free → Starter ($7/mo)** — fixes cold-start tail and makes in-process APScheduler reliable (free tier sleeps after 15min → jobs skip). User action.
- **Monetization follow-ups (deferred, from 2026-07-01)**: (1) `/fleet` message-count is an N+1 query (one `count` per tenant) — aggregate into one query; (2) AI-tier price constants are duplicated in `onboarding-wizard.tsx` and `create_client` — source from `feature_catalog.ai_tier.*.monthly_price`; (3) the existing pre-monetization tenant has no `tenant_subscriptions` row — backfill if it needs plan/usage in the console; (4) hard-cap quota enforcement intentionally deferred (metering is track-only, never blocks a send); (5) the monetization work is on `main` but **NOT pushed to origin** — `main` is ~10 commits ahead. Also: locked Feature Store cards still say "Toggle" in the description copy (minor UX nit).
- **`feature/scoring-booking-refactor` branch** — unmerged; contains booking state-machine removal + `[COLLECT_DONE]` pattern (migration 072 already applied). Dynamic pricing is already on main; the state-machine removal is NOT.
- **Operator console dark mode (DEFERRED by user 2026-07-02)** — approach fully decided + specced in `docs/plans/operator-console-qa-and-features.md` Task 10: migrate tailwind hex tokens → channel-triplet CSS vars (light values byte-identical), `.dark` override scoped to the operator console root only, toggle in operator-sidebar. Ready to execute on request.
- **Operator console deferred polish (from 2026-07-02 final review)**: (1) **`/alerts` needs a server-side cache** — the bell polls every 60s from every operator page and re-runs the fleet's 2-queries-per-tenant loop (compounds the existing `/fleet` N+1); (2) fleet CSV export lacks a formula-injection guard (`=+-@` name prefix); (3) palette "New Client" navigates to `/operator` but doesn't open the create wizard; (4) bulk action bar + summary pill share `fixed bottom-6 z-40` and can overlap; (5) `action-confirm.tsx` lacks Esc-close/focus-trap (matches confirm-dialog convention); (6) impersonation `/end` after tenant deletion is unaudited, and audit writes are best-effort (swallowed on DB failure — codebase-wide norm); (7) debounced audit search double-fetches when page>1; (8) alert severity "info" is typed but never emitted.
- ~~`main` unpushed AND behind origin~~ — **Resolved 2026-07-02**: reconciled and pushed (both directions). Surfaced a real bug during reconciliation — see "Production incident" in decisions/log.md — operator console top nav used a raw `<a href={item.href}>` that bypassed the new basePath. Fixed.
- **Hero section redesign — approved direction, not yet built (2026-07-02)**: brainstormed a "Live Console" hero for the landing page (`app/page.tsx`) to read as a technical product rather than a templated SaaS page — badge pill → mono live-status line (reusing `--success` token), static "dashboard mockup" screenshot → new `app/components/landing/HeroConsole.tsx` (dark terminal card reusing `--accent-gradient`, staggered mono event-log lines via the existing `.reveal`/`.reveal-delay-N` system, no new colors). User approved direction A before a production incident interrupted the session. Ready to implement on request — see the design writeup earlier in that session for exact copy/markup.
- **`maybe_single()` None-guard sweep (2026-07-02)** — `get_subscription` crashed on `AttributeError: 'NoneType' object has no attribute 'data'` because `maybe_single().execute()` returns `None` (not a response with `.data=None`) on zero rows; fixed there only. `client_overview`'s `tenant` and `owner_row` lookups in `operator.py` have the identical shape (`if not X.data:` without `if not X:` first) — no reported failure yet, but same landmine. Sweep and guard proactively.
- **Confirm apex domain `bloommatrix.in` (no www) reaches Aira (2026-07-02)** — the Vercel rewrite + basePath work was verified on `www.bloommatrix.in`. Meta's Tech Provider checklist lists the bare `bloommatrix.in/...` URLs (no www). Confirm the apex domain either redirects to `www` or has the same `/aira` rewrite applied directly — otherwise `bloommatrix.in/aira/privacy-policy` etc. 404 even though the www version works.
- **Confirm Vercel Deployment Protection off at the per-project level for Aira (2026-07-02)** — team-default "Vercel Authentication" was confirmed off (screenshot showed "Unprotected (0)"), which is sufficient for the Bloom Matrix → Aira rewrite proxy to work. Not separately confirmed inside the Aira project's own Settings → Deployment Protection page in case of a per-project override — low risk, quick to check.
- **SIM call tracking — dialing-path double-count (2026-07-02)** — the PWA cockpit's SIM-dial tap creates its own `sim_started` call_logs row; the new Android "Aira Sync" APK also creates its own row from the native call log. If a telecaller uses both (taps "Call" in the cockpit AND has the APK running), the same physical call can produce two rows. Not reconciled in Phase 1 — needs either (a) telling telecallers to dial from the native phone only and hiding the cockpit's SIM-dial button, or (b) matching-by-phone+timestamp reconciliation in `sim-cdr` ingestion so the APK's row updates the cockpit's existing row instead of inserting a new one. See [subsystem-notes.md](../context/subsystem-notes.md) → SIM call tracking.
- **SIM APK test-data cleanup (2026-07-02)** — lead `prem` (`b5a0a551-8c5c-4571-a68d-92035a9e5c9c`, `+919345679286`) was temporarily `assigned_to` the `Admin` caller (`13252cf0-06f8-4d3e-bbd5-277362a4cdde`) purely to end-to-end test the Aira Sync APK's lead-number filter. Unassign (or otherwise resolve) once testing is done — it isn't a real assignment.
- **SIM call tracking Phase 2 (recording upload)** — deliberately deferred from the 2026-07-02 Phase 1 build. Needs a `FileObserver` watcher on the OEM call-recorder folder (paths vary by manufacturer — Xiaomi/Samsung/OnePlus/Oppo confirmed different paths) in the Android APK, uploading matched recordings through the existing `_process_telecmi_recording`-style pipeline (reuse Groq transcription/summarization, just a different ingestion source).
- **Telegram save fix pushed but NOT confirmed live (2026-07-02)** — see decisions/log.md "Telegram settings save fixed" entry. Commit is on `origin/main`, but `aira-ai-5tfr` has `autoDeploy: no` (see the domain-migration incident entry), so a manual redeploy trigger (`mcp__render__update_environment_variables` no-op touch) + a live `list_deploys` check for status `"live"` on the right commit is still needed before telling the user it's fixed in production.

---

## Recently Completed Features
- **Operator Console QA + 6 features (2026-07-02, no migrations)**: auth-check resilience (unreachable→retry screen, not ejection), destructive-action confirms + masked temp passwords, custom `OperatorToggle`, shared `lib/operator.ts` + vitest, fleet attention queue with real health scoring, alert-center bell (`GET /operator/alerts`), Ctrl+K command palette, scheduler Run-now (409 on paused), bulk Suspend/Activate, read-only tenant impersonation (mint-nothing, security review PASS). Merged to `main` (`a4fa029`, **unpushed**). Plan: `docs/plans/operator-console-qa-and-features.md`. Backend 241/241, vitest 11/11.
- **SaaS Monetization + Entitlements (Migrations 123–126, 2026-07-01)**: `feature_catalog`/`plans`/`tenant_subscriptions`/`tenant_usage_counters` + track-only metering + 4-step onboarding wizard + `EntitlementToggle` (on/off/locked/metered ring) + Feature Store + Billing & Usage view + Fleet cockpit. Merged to `main` (`1620969`, **unpushed**); migrations applied to live Supabase + verified. Plan: `docs/superpowers/plans/2026-07-01-monetization-and-console.md`.
- **Callback notifications + configurable push (Migration 122, 2026-07-01)**: claimable-only model (auto-reassignment removed), due/claimable/claim pushes, tenant `notification_config` (per-event push toggles, threshold, audience incl. specific callers, quiet hours), admin "Notifications" settings tab. Merged to `main`; migration applied to live Supabase + verified. Plan: `docs/superpowers/plans/2026-06-30-callback-notifications.md`.
- **Security & Multi-Tenancy**: RLS launch blocker (Migration 114) + performance checks (Migration 115).
- **Shift Time Overrides**: Multi-tenant shift hour schedules per caller (Migration 112) + ShiftTimeline ranges.
- **Call Scripts Cockpit integration**: Segment-based routing, branch outcomes, and scripts cockpit panel (Migration 111).
- **Contact Recycling Engine**: Automatic APScheduler recycling job to re-queue unreachable voice leads (Migration 111).

## Tooling Follow-ups
- **Workspace root MUST be `Aira.AI\Aira-Ai` (inner), not the outer `Aira.AI`**: the git repo, `.claude/commands`, `.agents/`, `Makefile`, and all code live in the inner `Aira-Ai`. Opening Claude Code / the IDE at the outer `Aira.AI` makes `/aira-status` etc. invisible (Claude scans `<cwd>/.claude/commands`, which doesn't exist at the outer level) and breaks the commands' relative paths. The outer folder has its own separate (near-empty) `.git`/`.agents` — ignore it. Always open at the inner folder.
- **Local wiki refresh toolchain on Windows**: `graphify-out/` is now ignored/local-only, but this machine currently lacks `make` and `graphify` on PATH, so `/aira-wiki` cannot rebuild the wiki after a fresh clone/pull. Install/expose graphify + make, or add a Windows-friendly script target.

## Session Follow-ups
- **SIM Basic rollout checklist (Completed)**: The SIM manual analytics/status model, graceful push UX, provider-switch confirmation, provider-aware Live Agent Status, and mobile `tel:` dialer fix have been fully restored, fixed, and pushed to `main` (commit `91abfe2`). Testing issues (unit tests/Windows encoding) are resolved. Migrations `120_calling_provider_and_push.sql` and `121_sim_manual_call_statuses.sql` have been applied to live Supabase (2026-06-29). Mobile backgrounding fixes (keepalive, sync context, lazy call log creation, bottom-sheet modal) pushed to `main`.
- **VAPID keys on Render (Action Required)**: VAPID keys have been generated and added to `backend/.env` locally. To enable push notifications in production, manually add `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, and `VAPID_SUBJECT` to the Render Dashboard environment variables. Also ensure `pywebpush` is in `requirements.txt` and installed on the Render backend.
- **Telecaller invite provider-aware fields**: TeleCMI requires agent id/password; SIM Basic needs only name, email, password, mobile number, plus optional SIM label, shift hours, or targets. Currently the invite form is not provider-aware.
- **Operator audit — label 2nd developer by email (deferred)**: the global Operator audit log (`/api/v1/operator/audit-logs`) filters `actor_role='system_admin'` (developer/operator activity only; tenant events stay in the per-tenant view). Aira has two owners; when a second `system_admin` id is added, label each developer in the Actor column by **email** (resolve `actor_user_id` via Supabase auth — no schema change; `system_admins` has only user_id + created_at). User chose email over a custom name column.
