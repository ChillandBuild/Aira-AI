{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "permissions": {
    "allow": [
      "mcp__render__list_logs",
      "mcp__claude_ai_Supabase__execute_sql",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(python -m pytest tests/test_templates.py -q)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '💾 SESSION END — do both before closing:\\n1. Save anything worth remembering from this session (new decisions, fixes, patterns, corrections)\\n2. Audit all memory files — update stale facts, delete anything no longer true, merge duplicates'"
          },
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-/Users/prem/Documents/Aira AI}\" 2>/dev/null && N=$(find backend/app frontend/app -type f \\( -name '*.py' -o -name '*.ts' -o -name '*.tsx' \\) -newer graphify-out/manifest.json 2>/dev/null | wc -l | tr -d ' ') && [ \"${N:-0}\" -gt 15 ] && echo \"🗺️  WIKI STALE — $N source files changed since the last graphify build. Run /wiki to refresh graphify-out/wiki/ before it drifts from the code.\" || true"
          }
        ]
      }
    ]
  }
}
